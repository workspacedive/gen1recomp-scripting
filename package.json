{
  "name": "gen1recomp-scripting",
  "version": "0.4.3",
  "private": true,
  "description": "Gen1Recomp \u2192 Scripting iOS (Scripting.fun) \u2014 Architektur + Host Adapter (Non-Pro, P0 Scaffold)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --ext .ts,.tsx || true",
    "test": "vitest run || true"
  },
  "dependencies": {
    "fengari": "^0.1.4",
    "fengari-interop": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0",
    "fast-check": "^3.20.0",
    "jszip": "^3.10.0"
  }
}
